export const metadata = { title: "Link Click Tracker · MNA Content" };

export default function LinkTrackerPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Link Click Tracker</h1>
      <p className="mt-2 max-w-2xl text-sm text-muted">
        Phase 1 stub — track outbound / bio link clicks per workspace. Schema
        extension planned after Monitor Data sync pipeline is stable.
      </p>
    </div>
  );
}
