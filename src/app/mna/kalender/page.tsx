export const metadata = { title: "Kalender Editorial · MNA Content" };

export default function KalenderPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Kalender Editorial</h1>
      <p className="mt-2 max-w-2xl text-sm text-muted">
        Phase 1 stub — calendar/list of content due dates filtered by active
        workspace (Jeparanesia / Siinbooth). Wire to <code>contents</code> table
        next.
      </p>
    </div>
  );
}
